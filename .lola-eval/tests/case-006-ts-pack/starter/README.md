# Order API

A simple user and order management API built with Express and TypeScript.

## Endpoints

- `GET /users` — list all users
- `POST /users` — create a user
- `GET /users/:id` — find a user by ID
