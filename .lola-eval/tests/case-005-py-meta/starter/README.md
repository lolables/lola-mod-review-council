# TaskQ

A fast, lightweight task queue.

**Goals:** Simple API, reliable delivery, no external dependencies.
We deliberately avoid complex features like priority queues or
delayed tasks to keep the codebase minimal and auditable.
