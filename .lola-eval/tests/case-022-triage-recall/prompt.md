Review this Go codebase using `/review-council deep main..feat --triage`.
Report all findings.

The review-council module is pre-installed in this project's `.lola/`
directory. Use deep effort so the changeset is decomposed into subsystems, and
enable subsystem triage so each persona is dispatched only to the subsystems
the triage pass judges relevant to it.

Report every finding the council returns, and state which persona/subsystem
pairs triage excluded.
